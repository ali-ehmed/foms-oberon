$("#fetched_invoices").hide().html("<%= escape_javascript(render 'fetching_invoices').html_safe %>").fadeIn(500);

$(".best_in_place").best_in_place()
$('[data-toggle="tooltip"]').tooltip()



$("#invoice_date").datepicker({
  format: 'M d, yyyy',
  orientation: 'bottom left'
}).on('changeDate', function(e) {
  return $(this).datepicker('hide');
});


// $table = $("#invoices_table tbody");
// $rows = $table.find("tr")

// $.each($rows, function() {
// 	$(this).find("td").each(function(){
// 		if($(this).find("a").length) {
// 			$(this).find("a").addClass("editable");
// 		} else {
// 			$(this).addClass("editable");
// 		}
// 	});
// });

// var tab_count = 0;
// $.each($rows, function(){
// 	var elem;
// 	elem = $(this).find(".editable")
// 	elem.each(function(){
// 		$(this).attr("tabindex", tab_count += 1)
// 	});
// });





// $.fn.editable.defaults.mode = 'popup';
// $.fn.editable.defaults.ajaxOptions = {type: "PUT"};
// $('#percent_allocation').editable();